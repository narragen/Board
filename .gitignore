# dependencies
node_modules/

# builds
dist/
build/
*.tsbuildinfo

# local dev data (dev daemon / tests use a temp or in-repo data dir)
.data/

# env
.env
.env.*
!.env.example

# local agent token for dogfooding (never commit — invariant 7, tokens stored hashed)
.board-token

# logs & OS noise
*.log
.DS_Store
Thumbs.db

# browser tooling session junk (playwright MCP)
.playwright-mcp/

# editors
.idea/
.vscode/*
!.vscode/extensions.json
